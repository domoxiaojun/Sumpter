**
# Thin runtime image; the context is this directory
# (release.yml container / publish jobs build it)
!Dockerfile.runtime
!docker-bin/
!docker-bin/**
# Shared runtime assets
!config.example.json
!web/
!web/**
