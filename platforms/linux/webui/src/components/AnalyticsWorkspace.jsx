// Canonical Analytics v3 workspace entry point. The implementation remains in
// the historical file for one release so external integrations can migrate
// without a blank Statistics page.
export { AnalyticsWorkspace } from './AnalyticsV2Workspace.jsx';
