# 多平台项目工程化与新仓库准备

## Pi wrapper 归因入口（2026-09-08）

- ✅ 核实已有 Pi 请求头扩展与本机 Pi 的扩展加载接口。
- ✅ 增加统一脚本 `run pi`，复用动态归因扩展，补齐非 origin 远程仓库回退与用户名验证。
- ✅ 同步两端资源和使用说明，运行定向测试、文档检查与链接校验。

范围：只修改 Sumpter 的 Pi 启动与归因资源；Codex 暂不修改，不安装或发布。

## Kekulv Linux 迁移到 Sumpter（2026-09-08）

目标：为 `/var/lib/kekulv` 的 system 安装提供独立迁移脚本，保留配置、登录凭据和统计；新服务验收成功后卸下旧程序，失败恢复旧服务。

- ✅ 核实旧远程安装/卸载脚本、当前 system 安装布局及配置迁移支持。
- ✅ 实现下载校验、停服复制、权限转换、服务验收和失败回滚；补充使用说明与打包入口。
- ✅ 12 项隔离迁移测试通过，覆盖成功、下载/校验/安装/健康检查/停服/旧程序移走失败、原服务未启动、重复执行、路径冲突与数据保留；现有安装器 user/system 事务自测、ShellCheck、actionlint、docs gate（7 项）、README 本地链接和差异检查通过。

边界：仅制作并验证脚本，不执行用户服务器迁移，不发布或覆盖静态镜像；保留其他任务的改动。

交付：`platforms/linux/scripts/migrate-kekulv.sh` 可独立复制到旧服务器运行，默认下载 GitHub latest，也支持 `--version v0.3.7`。真实 Linux systemd、SELinux 权限转换和业务代理请求尚未在用户服务器验证。迁移测试合入现有 installer 检查步骤，未增加 CI job。

目标：以现有 Linux / macOS 共享引擎为基础，统一项目文档、开发检查、协作和发布流程，为不继承旧 Git 历史的新仓库准备可验证的源码快照。

- ✅ 盘点目录、文档、工作区和提交作者；确认 Claude 来自两条 Co-Authored-By，用户将自行删除并重开远端仓库。
- ✅ 建立正式项目入口、贡献与安全规则、统一配置/开发/发布/迁移文档；整理历史文档和重复入口。
- ✅ 统一可执行开发检查与 CI，补齐 macOS 验证、文档契约和项目模板。
- ✅ 准备不包含旧历史及本地运行数据的源码导出工具和新仓库操作说明。
- ✅ 运行最小必要检查，修复本轮问题，生成并核验新仓库源码快照，记录交付边界。

边界：保留现有业务行为、客户端支持、MIT 许可证和必要历史说明；不删除当前 .git，不改写或推送旧远端，不发布版本。新仓库地址未指定，仓库身份暂沿用 domoxiaojun/sumpter。

## 本次验证与交付

- ✅ 文档与资源同步、7 项工程/导出契约测试通过；导出目录独立执行 docs gate 通过。
- ✅ WebUI 135 项测试、客户端归因脚本 14 项测试及生产构建通过，受控 Web 产物无差异。
- ✅ 根工作流 actionlint、统一脚本 shellcheck、git diff --check、现行文档本地及外部链接检查通过。
- ✅ 新源码目录 `/Users/kkl/Documents/claude/sumpter-new-repo-20260908`：398 个文件/内部链接，旁置 SHA-256 清单逐项核验，无 .git；gitleaks 脱敏扫描无发现。
- ✅ 原仓库与远端历史保留，未提交、push、创建远端、发布或安装。业务 Rust/Swift 未改，本轮未运行完整 Rust/Swift 构建测试；新增 CI 仍需在新远端验证。

后续由维护者按 `docs/repository-migration.md` 创建全新首提交与空远端，配置分支保护、Secrets、Sparkle 编号基数和更新地址。旧仓库贡献者页面本次未改变；新仓库不能推入旧分支或标签历史。

## 独立发布任务：v0.3.4

- ✅ 已将发布准备与客户端归因功能合并提交为 `9b411429315942d055655da66894213ba711ab19`。
- ✅ main 与 v0.3.4 标签已推送，远端解引用提交一致。
- ✅ 已启动 Release 构建，运行 ID：34177803470；以已提交标签为发布输入，之后的工程化迁移工作区改动未包含其中。
- [ ] 等待平台构建及统一发布完成，核对公开资产。

## scripts 目录整理

- ✅ 盘点脚本引用与生成副本边界。
- ✅ 按 clients、maintenance、tests 分组，更新源码引用、CI 和文档，保留平台安装包布局。
- ✅ 本轮 14 项客户端测试、7 项仓库/导出测试、文档与资源同步、shellcheck、actionlint、文档本地链接及 git diff --check 通过；未运行完整 Rust/Swift 构建或重新打包。

## 仓库公开准备：全项目整理

- ✅ 盘点根目录、文档、工作流、许可与源码导出边界。
- [ ] 分离历史资料与生成模板，补齐目录职责和公开流程。
- [ ] 加强本地文件排除与资源一致性检查。
- [ ] 验证现行文档、脚本及源码导出，记录实际公开前的外部步骤。

## pi 归因安装交互补齐

目标：pi 与其他客户端共用检查、安装和还原入口，显示扩展的实际安装状态；安装前保留原文件，操作后提示在 pi 执行 `/reload`。

- ✅ 核对截图与现行实现，确认 pi 仅展示内置资源，尚未接入统一安装器。
- ✅ 为统一安装器增加 pi 扩展的状态、安装、更新与还原，补齐 Linux 下载流程。
- ✅ 更新 macOS 与 Linux 的归因面板及使用说明，移除仅复制文件的旧安装入口。
- ✅ 隔离目录客户端测试 17 项、Swift 内置安装器 3 项测试通过，归因资源已同步；WebUI 构建与发布验证继续在 v0.3.6 步骤执行。

边界：保留本轮开始前的工程化整理改动；不修改用户的真实 pi、shell 或 provider 配置，不提交、push 或替换已安装 App。

## 项目结构与文档整理（2026-09-08）

目标：让维护者能按需求找到源码、测试和文档，明确共享模块、平台产品、生成副本与历史资料的职责，减少重复维护的目录树和命令说明。

- ✅ 盘点当前目录、未提交改动、文档入口与同步工具，确认现有脚本分类和历史归档。
- ✅ 建立项目结构指南，核对源码、测试、平台资源和生成文件的实际归属。
- ✅ 整理 README 与文档导航，收敛架构、开发和脚本说明中的重复内容；补充 CI 文档入口与章节锚点检查。
- ✅ 运行文档契约、相关链接与差异检查，修复 Linux/macOS 的旧章节链接，记录本轮验证和剩余边界。

边界：接续现有结构，不搬动业务源码或改变运行行为；保留其他任务的工作区改动及待办，不提交、push、导出新仓库或发布。

本轮结果：新增 `docs/project-structure.md`，文档索引按使用、开发、版本与仓库维护分类；架构页收敛目录树和重复命令，开发与脚本说明明确维护源、生成副本和测试归属。CI 纳入新文档并检查章节锚点。

验证：`./scripts/check.sh docs` 通过（7 项测试及文档/资源同步）；现行 CI 文档列表的 lychee 本地路径与锚点检查通过（147 个链接项，0 错误，3 项按离线模式排除）；`actionlint .github/workflows/ci.yml` 和 `git diff --check` 通过。本轮只改文档与 CI 链接检查，未运行业务 Rust/Swift/WebUI 构建测试，也未验证外部链接、安装、发布或部署。

## GitHub 工作流收敛（2026-09-08）

- ✅ 删除与发布流程重复的 PR 容器镜像工作流 `container.yml`。
- ✅ 删除独立离线文档链接 job，保留仓库契约检查并按需手动执行链接检查。
- ✅ 同步分支保护、开发指南和发布边界说明。
- ✅ 运行 workflow 语法、文档契约和差异检查。

## 修复 v0.3.5 提交的 CI 失败（2026-09-08）

- ✅ 读取 CI 失败日志：WebUI 产物未同步，Rust 测试通过但 Clippy 失败。
- ✅ 重建受版本控制的 WebUI 产物。
- ✅ 修复 Rust 1.88 Clippy 报错；全 workspace Clippy、格式检查、core/runtime 259 项测试及客户端脚本 14 项测试通过。
- ✅ 修复提交 8163fde 已推送；CI 34196136617 的四个 job 全部成功。

边界：现有 v0.3.5 正式标签保持不变，当前未发现 Release 运行；CI 修复完成不等于发布完成。

## v0.3.6 发布（2026-09-08）

- ✅ 补齐 macOS pi 一键安装及 Linux 远程管理，修复已报告 CI 失败。
- ✅ 更新版本、锁文件、文档及 WebUI 产物；Rust 1.88 统一检查、macOS 197 XCTest + 34 Swift Testing、WebUI 135 项、客户端 17 项、文档契约 7 项及 Shell/TOML/文档链接检查通过。
- ✅ 提交 97f5f5b 已推送，CI 34197254163 的四个 job 全绿；v0.3.6 标签指向该提交。
- ✅ Release 34197575223 已结束：Linux 和容器成功，macOS 编译/签名成功但 diskutil 生成 DMG 失败；Publish 跳过，未对外发布。

## v0.3.8 发布（2026-09-08）

- [ ] 收进 WebUI 不安全上下文修复、Kekulv 迁移、归因文档与 pi wrapper。
- [ ] 版本升到 0.3.8，CI 绿后推送 `v0.3.8` 并核对 Release。

## 现行文档对齐 0.3.7 / schema v7（2026-09-08）

- ✅ README 写清版本、GitHub 安装、客户端、默认端口和 CI/Release 分工。
- ✅ USAGE、Linux/macOS 说明、文档索引与路径矩阵去掉过期的独立归因脚本和仅 CC/Codex 口径。

## 使用说明改为仓库链接（2026-09-08）

- ✅ 安装/卸载与归因脚本默认使用 GitHub Release 和仓库 raw。
- ✅ 替换过期的独立 CC/Grok 配置器使用说明与 WebUI 复制命令。
- ✅ 同步 USAGE 模板、WebUI 产物并提交。

## v0.3.7 打包修复与发布（2026-09-08）

- ✅ 定位 diskutil 能力探测误判，优先使用已保留的 hdiutil HFS+ / UDZO 路径。
- ✅ 本机正式打包（Rust 1.88 + Swift release）生成 App/DMG/ZIP，挂载及包内内容验证通过；版本同步、文档/Shell/TOML 检查通过。修复提交 2163486 已推送，CI 34198583216 四个 job 全绿。
- ✅ 已推送 `v0.3.7`，Release 34198873455 成功。后续改动改发 0.3.8，不改写该 tag。

授权承接“按顺序做一下”；不改写已有正式标签，不安装替换本机 App。


## 移除 Git 历史中的 Claude 共同作者（2026-09-08）

目标：按用户授权清除 main 的两条 Claude 共同作者署名并更新远端，保留代码、实际作者、未提交工作和正式版本标签。

- ✅ 核对本地与远端 main、两条署名、现有标签和 Actions；在仓库外备份历史与未提交改动。
- ✅ 在独立副本中仅改写两条署名，27 条提交的代码树、作者、时间与提交关系全部核验通过。
- ✅ 关闭本仓库的 Claude 自动署名，使用远端原 SHA 保护推送并同步本地分支；清理后的提交为 a33e60d。
- ✅ 远端当前 main 的 28 条提交无 Claude 共同作者署名；其他任务已在 a33e60d 上提交 97f5f5b，业务改动完整保留，现有正式标签未改写。GitHub 贡献者侧栏异步加载，显示刷新尚未验证。

备份目录：/Users/kkl/.codex/backups/sumpter/remove-claude-20260908T065305Z
